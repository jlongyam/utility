(function(w,d){
  var has_body = ('body' in document);
  if(typeof display === 'undefined' ) display = {}
  
  display.string = function(s) {
    if( has_body ) {
      
    }
    else {
      // require dom.evenListener
    }
  }
  w.display = display
}(window,document))